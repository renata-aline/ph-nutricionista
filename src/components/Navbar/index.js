import MenuIcon from "@mui/icons-material/Menu";

const Navbar = () => {
  return (
    <nav className="navbar">
      <button>
        <MenuIcon />
      </button>
    </nav>
  );
};

export default Navbar;
